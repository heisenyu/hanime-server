services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hanime-server
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data/downloads:/app/downloads
      - ./data/logs:/app/logs
    ports:
      - "${APP_PORT:-7788}:7788"
